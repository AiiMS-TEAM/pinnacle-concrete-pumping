# Block direct HTTP access to dotfiles (.env, .git, etc.)
<FilesMatch "^\.">
    Require all denied
</FilesMatch>

# Belt-and-braces: explicitly deny .env in case the rule above is overridden.
<Files ".env">
    Require all denied
</Files>
